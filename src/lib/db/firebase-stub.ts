// Empty stub module used as a resolve alias for firebase/* packages
// when the firebase provider is not selected. This prevents build
// failures when the firebase npm package is not installed.
export {};
